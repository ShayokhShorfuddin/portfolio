import type { MDXComponents } from "mdx/types";

const components: MDXComponents = {
  // TODO: Need to add stuff here later
};

export function useMDXComponents(): MDXComponents {
  return components;
}
