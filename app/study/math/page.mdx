import Image from "next/image";
import solvingUnsolvableEquationsUsingImaginaryNumber from "@/public/solving-unsolvable-equations-using-imaginary-number.jpg";

# Welcome to my MDX page!

This is some **bold** and _italics_ text <sub>Hh</sub>

This is a list in markdown:

- One
- Two
- Three

And here is some code:

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}
```

## Math Equations

Inline math: $E = mc^2$

Block math:

$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$

<Image
  src={solvingUnsolvableEquationsUsingImaginaryNumber}
  alt="Solving unsolvable equations using imaginary numbers."
  width={600}
/>

{/* TODO: That imaginary number image needs some cropping */}
{/* TODO: Need to bring in fonts. Check discussion with claude */}
